#include <stdio.h>

int main()
{
    int n,k,c;
    scanf("%d %d",&n,&k);
    c=n-k;
        if(c%2==0)
        {
        printf("even");
        }
        else
        {
            printf("odd");
            
        }
    return 0;
}
